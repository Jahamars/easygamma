name: Release AppImage

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            cmake g++ pkg-config \
            libgtkmm-3.0-dev \
            libwayland-dev \
            wayland-protocols \
            libayatana-appindicator3-dev \
            libfuse2

      - name: Configure
        run: |
          cmake -S . -B build \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_INSTALL_PREFIX=/usr

      - name: Build
        run: cmake --build build --parallel

      - name: Install into AppDir
        run: DESTDIR=${{ github.workspace }}/AppDir cmake --install build

      - name: Manually copy icon into AppDir
        run: |
          mkdir -p AppDir/usr/share/icons/hicolor/scalable/apps
          cp easygamma.svg AppDir/usr/share/icons/hicolor/scalable/apps/easygamma.svg

      - name: Verify AppDir contents
        run: find AppDir -type f

      - name: Download linuxdeploy + GTK plugin
        run: |
          curl -fsSL -o linuxdeploy-x86_64.AppImage \
            "https://github.com/linuxdeploy/linuxdeploy/releases/download/1-alpha-20240109-1/linuxdeploy-x86_64.AppImage"
          curl -fsSL -o linuxdeploy-plugin-gtk-x86_64.AppImage \
            "https://github.com/linuxdeploy/linuxdeploy-plugin-gtk/releases/download/continuous/linuxdeploy-plugin-gtk-x86_64.AppImage"
          chmod +x linuxdeploy-x86_64.AppImage linuxdeploy-plugin-gtk-x86_64.AppImage

      - name: Build AppImage
        env:
          DEPLOY_GTK_VERSION: 3
          VERSION: ${{ github.ref_name }}
        run: |
          ./linuxdeploy-x86_64.AppImage \
            --appdir AppDir \
            --plugin gtk \
            --desktop-file AppDir/usr/share/applications/easygamma.desktop \
            --icon-file AppDir/usr/share/icons/hicolor/scalable/apps/easygamma.svg \
            --output appimage

      - name: Rename AppImage with version
        run: mv EasyGamma*.AppImage EasyGamma-${{ github.ref_name }}-x86_64.AppImage

      - name: Create GitHub Release and upload AppImage
        uses: softprops/action-gh-release@v2
        with:
          files: EasyGamma-${{ github.ref_name }}-x86_64.AppImage
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
